'use strict';

describe('Service: services/data/models', function () {

  // load the service's module
  beforeEach(module('modelsstockApp'));

  // instantiate service
  var services/data/models;
  beforeEach(inject(function (_services/data/models_) {
    services/data/models = _services/data/models_;
  }));

  it('should do something', function () {
    expect(!!services/data/models).toBe(true);
  });

});
